{
  "name": "botan",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cd next && bun run dev",
    "build": "cd next && bun run build",
    "start": "cd next && bun run start",
    "lint": "cd next && bun run lint",
    "tauri": "tauri"
  },
  "workspaces": [
    "next"
  ],
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11"
  },
  "dependencies": {
    "@tauri-apps/plugin-store": "~2"
  }
}
